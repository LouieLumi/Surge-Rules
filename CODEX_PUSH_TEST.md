# Local Git push test

This file verifies the local editing, Git commit, and Git push workflow for
LouieLumi/Surge-Rules.

The test is isolated on the `codex/push-test-20260917` branch.
The existing Surge rules are unchanged.
